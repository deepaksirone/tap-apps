if(Reddit.newPostInSubreddit.Title.indexOf("1600") >= 0) {
   // give you a notification
}else{
  // don't send a notification
}
if(Reddit.newPostInSubreddit.Title.indexOf("Ryzen") >= 0) {
   // give you a notification
}else{
  // don't send a notification
}